# Development
#spring.datasource.url=${LOCAL_URL}
#spring.datasource.username=${LOCAL_USER}
#spring.datasource.password=${LOCAL_PASSWORD}

spring.datasource.url=jdbc:postgresql://${DB_HOST:host.docker.internal}:${DB_PORT:5432}/${DB_NAME:DND-Tool}
spring.datasource.username=${AWS_DB_USER:postgres}
spring.datasource.password=${AWS_DB_PASSWORD:12345}
spring.datasource.DriverClassName=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

server.port=${SERVER_PORT:8080}

spring.application.name=DND-Encounter-Tool
